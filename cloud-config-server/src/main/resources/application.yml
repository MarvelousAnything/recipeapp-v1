server:
  port: 9296

spring:
  application:
    name: CONFIG-SERVER
  cloud:
    config:
      server:
        git:
          uri: https://github.com/thearchermancoding/config-server
          clone-on-start: true
eureka:
  instance:
    ip-address: 192.168.0.122
    prefer-ip-address: true